# `neu-morpho`

From `neu-morpho`. Meshes and skeletons from a segmentation volume.

`python -m neu_morpho` is equivalent.

```{argparse}
:module: clitools
:func: em_morpho_parser
:prog: neu-morpho
```
