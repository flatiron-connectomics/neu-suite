# CLI reference

Every page here is rendered from the `ArgumentParser` the command actually runs, so it
cannot disagree with `--help`. If a flag is described here, that is the description the
terminal prints.

For a one-page overview instead, see the [cheat sheet](../_generated/cheatsheet.md).

```{toctree}
:maxdepth: 1

em-vol
em-morpho
em-annot
```
