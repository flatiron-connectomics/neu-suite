# `em-vol`

From `em-volume-tools`. Everything to do with the volumes themselves.

`python -m em_volume_tools` is equivalent, which is occasionally useful when you want to
be certain which installation you are running.

```{argparse}
:module: clitools
:func: em_vol_parser
:prog: em-vol
```
