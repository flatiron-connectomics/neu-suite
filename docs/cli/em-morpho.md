# `em-morpho`

From `em-seg-morpho`. Meshes and skeletons from a segmentation volume.

`python -m em_seg_morpho` is equivalent.

```{argparse}
:module: clitools
:func: em_morpho_parser
:prog: em-morpho
```
