# `neu-vol`

From `neu-vol`. Everything to do with the volumes themselves.

`python -m neu_vol` is equivalent, which is occasionally useful when you want to
be certain which installation you are running.

```{argparse}
:module: clitools
:func: em_vol_parser
:prog: neu-vol
```
